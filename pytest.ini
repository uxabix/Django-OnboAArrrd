[pytest]
DJANGO_SETTINGS_MODULE = OnboAArrrd.settings
python_files = test_*.py
testpaths =
    accounts/tests
    onboarding/tests
    chat/tests
addopts = -ra --strict-markers
markers =
    django_db: Tests that use the Django database (pytest-django).
